import type { Chain } from '@wagmi/core';
import { InjectedConnector } from '@wagmi/core/connectors/injected';
interface Info {
    uuid: string;
    name: string;
    icon: string;
    rdns: string;
}
interface ConnectOptions {
    rdns: Info['rdns'];
    chainId?: number;
}
interface Config {
    chains?: Chain[];
}
export declare class W3mAnnouncedConnector extends InjectedConnector {
    #private;
    readonly id = "w3mAnnounced";
    readonly name = "w3mAnnounced";
    constructor(config: Config);
    connect(options: ConnectOptions): Promise<{
        account: `0x${string}`;
        chain: {
            id: number;
            unsupported: boolean;
        };
    }>;
    disconnect(): Promise<void>;
    listenForWallets(): void;
}
export {};
